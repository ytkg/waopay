<% if @error %>
  $('.modal-body .amount').html('<%= @error %>')
<% else %>
  $('.modal-body .amount').html('<%= number_to_currency(@amount) %>')
  $('.modal-body .pay').html('<%= "#{@user.name}(#{@user.username})さん" %>')
<% end %>
$('button#openPayModalButton').click()
